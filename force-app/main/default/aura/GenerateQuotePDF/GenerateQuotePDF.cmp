<aura:component controller="QuotePdfController"
                implements="force:appHostable,flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId,forceCommunity:availableForAllPageTypes,force:lightningQuickAction" access="global" >
	<aura:attribute name="recordId" type="string"/>
    <aura:attribute name="LeadRecord" type="Object"/>
    <aura:attribute name="bankName" type="string"/>
    <aura:attribute name="recordTypeName" type="String"/>
    <force:recordData aura:id="recordLoader"
                  recordId="{!v.recordId}"
                  fields="Name,OwnerId,RecordType.Name"
                  recordUpdated="{!c.doInit}"
                  targetFields="{!v.LeadRecord}"
                  targetError="{!v.recordLoadError}"
                  />
    <aura:html tag="style">
        
        .panel.slds-modal > .modal-container{
        width: 75%;
        max-width: 70rem;
        }
    </aura:html>
    <section class="slds-modal slds-fade-in-open">
        <header class="slds-modal__header">
            <h2 class="slds-text-heading_medium slds-hyphenate">Generate Cost Break UP Letter({!v.recordTypeName})</h2>
        </header>
            <div class="slds-modal__content slds-p-around_medium" id="modal-content-id-1">
              <aura:if isTrue="{!v.recordTypeName == 'Apartments'}">
        <iframe src="{!'/apex/apex/ZuariCostSheet?Id=' + v.recordId}" width="100%" height="500px;" frameBorder="0"/>
    <aura:set attribute="else">
        <iframe src="{!'/apex/apex/CostSheet?Id=' + v.recordId}" width="100%" height="500px;" frameBorder="0"/>
    </aura:set>
    </aura:if></div>
        
        <footer class="slds-modal__footer">
                        <button class="slds-button slds-button_neutral" onclick="{!c.close}">Cancel</button>
            <button class="slds-button slds-button_brand" onclick="{!c.send}">Send Email</button>
            
        </footer>
    </section>
</aura:component>