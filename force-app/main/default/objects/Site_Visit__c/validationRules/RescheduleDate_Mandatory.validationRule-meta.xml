<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RescheduleDate_Mandatory</fullName>
    <active>false</active>
    <description>RescheduleDate Mandatory when status is changed to Reschedule</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status__c , &apos;Rescheduled&apos;),
    OR(
        ISBLANK(Reschedule_Date__c),
        Reschedule_Date__c &lt;= NOW()
    )
)</errorConditionFormula>
    <errorMessage>Please select a future Reschedule Date.</errorMessage>
</ValidationRule>
